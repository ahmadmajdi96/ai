import numpy as np

def embed_text(t: str):
    # Placeholder; replace with actual embedding model like bge-large or e5-large
    vec = np.random.default_rng(0).normal(size=(1024,))
    return vec.tolist()
